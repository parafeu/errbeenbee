{% extends 'base.html.twig' %}

{% block title %}Availability index{% endblock %}

{% block body %}
    <h1>Availability index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>StartDate</th>
                <th>EndDate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for availability in availabilities %}
            <tr>
                <td>{{ availability.id }}</td>
                <td>{{ availability.startDate ? availability.startDate|date('Y-m-d') : '' }}</td>
                <td>{{ availability.endDate ? availability.endDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('availability_show', {'id': availability.id}) }}">show</a>
                    <a href="{{ path('availability_edit', {'id': availability.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('availability_new') }}">Create new</a>
{% endblock %}
