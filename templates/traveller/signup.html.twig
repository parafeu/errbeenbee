{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .container{
            width: 100vw;
            height: 100vh;
        }
    </style>
    <div class="container">

    </div>

    {{ include('traveller/_form.html.twig') }}

    <a href="{{ path('traveller_index') }}">back to list</a>
{% endblock %}
